apiVersion: v1
kind: Secret
metadata:
  name: gaudi3-scale-secrets
  namespace: gaudi3-scale
  labels:
    app.kubernetes.io/name: gaudi3-scale
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database credentials (to be overridden in production)
  DATABASE_URL: "postgresql://gaudi3:changeme@postgres:5432/gaudi3_scale"
  DATABASE_USER: "gaudi3"
  DATABASE_PASSWORD: "changeme"
  
  # Redis credentials
  REDIS_URL: "redis://redis:6379/0"
  REDIS_PASSWORD: ""
  
  # Application secrets
  SECRET_KEY: "your-secret-key-here"
  JWT_SECRET: "your-jwt-secret-here"
  
  # External service API keys
  WANDB_API_KEY: ""
  SLACK_WEBHOOK_URL: ""
  GITHUB_TOKEN: ""
  
  # Monitoring credentials
  GRAFANA_ADMIN_PASSWORD: "admin"
  PROMETHEUS_USER: "prometheus"
  PROMETHEUS_PASSWORD: "prometheus"
  
  # Cloud provider credentials (to be set via external secret management)
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
  AZURE_CLIENT_ID: ""
  AZURE_CLIENT_SECRET: ""
  GCP_SERVICE_ACCOUNT_KEY: ""